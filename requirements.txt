fastapi
uvicorn

# MongoDB (async)
motor
pymongo[srv]

# Auth / JWT
python-jose
python-jose[cryptography]

# Password hashing
passlib[argon2]

# Upload de fichiers
python-multipart

# Email validation (pour EmailStr)
email-validator

# CORS si tu l'utilises
starlette

# Utilisé par FastAPI (garde-le)
pydantic

# Si tu veux gérer les dates proprement
python-dateutil

# Si tu utilises dotenv (facultatif)
python-dotenv
